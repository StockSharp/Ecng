<dx:ThemedWindow
		x:Class="Ecng.Xaml.Database.DatabaseConnectionCreateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:db="clr-namespace:Ecng.Xaml.Database"
        xmlns:xaml="clr-namespace:Ecng.Xaml"
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        Title="New connection string" ShowInTaskbar="False"
		Width="700" Height="500" WindowStartupLocation="CenterOwner"
		ResizeMode="NoResize">
    <Grid Margin="5">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="100" />
			<ColumnDefinition Width="*" MinWidth="300" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition MinHeight="240" />
			<RowDefinition Height="5" />
			<RowDefinition Height="24" />
		</Grid.RowDefinitions>

		<db:DatabaseConnectionPanel x:Name="SettingsGrid" Grid.ColumnSpan="3" />

		<Button x:Name="CheckCtrl" Grid.Row="2" Grid.Column="0" Click="TestCtrl_Click">Check</Button>
		<Button x:Name="Ok" Grid.Row="2" Grid.Column="1" Width="100" IsEnabled="False" IsDefault="True" xaml:XamlHelper.DialogResult="True" HorizontalAlignment="Right">OK</Button>
	</Grid>
</dx:ThemedWindow>
