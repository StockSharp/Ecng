<UserControl x:Class="Ecng.Xaml.DevExp.EndPointListEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:devExp="clr-namespace:Ecng.Xaml.DevExp"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors">

    <Control.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <DockPanel LastChildFill="True">
                            <Border BorderBrush="Red" BorderThickness="1">
                                <AdornedElementPlaceholder />
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip"
							Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <devExp:EndPointListConverter x:Key="EndPointToString" />
    </Control.Resources>

    <dxe:TextEdit x:Name="Address"
				  BorderThickness="{Binding RelativeSource={RelativeSource AncestorType={x:Type devExp:EndPointListEditor}}, Path=BorderThickness}"
				  BorderBrush="{Binding RelativeSource={RelativeSource AncestorType={x:Type devExp:EndPointListEditor}}, Path=BorderBrush}">
        <dxe:TextEdit.Text>
            <Binding Path="EndPoints"
					 RelativeSource="{RelativeSource AncestorType={x:Type devExp:EndPointListEditor}}"
					 Mode="TwoWay"
					 NotifyOnValidationError="True"
					 UpdateSourceTrigger="PropertyChanged" 
					 Converter="{StaticResource EndPointToString}">
                <Binding.ValidationRules>
                    <devExp:EndPointValidationRule Multi="True" />
                </Binding.ValidationRules>
            </Binding>
        </dxe:TextEdit.Text>
    </dxe:TextEdit>
</UserControl>
