<UserControl x:Class="Ecng.UI.Card"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    Width="200" Height="300" MouseEnter="UserControl_MouseEnter" MouseLeave="UserControl_MouseLeave" MouseLeftButtonUp="UserControl_MouseLeftButtonUp">
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Rectangle x:Name="Picture" HorizontalAlignment="Left" Width="36" Height="36" StrokeThickness="2" Grid.Row="0">
            <Rectangle.Fill>
                <ImageBrush />
            </Rectangle.Fill>
            <Rectangle.Stroke>
                <SolidColorBrush Color="Yellow" />
            </Rectangle.Stroke>
        </Rectangle>
        <TextBlock x:Name="Description" HorizontalAlignment="Left" TextWrapping="Wrap" Grid.Row="1" />
    </Grid>
</UserControl>
